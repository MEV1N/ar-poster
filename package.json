{
  "name": "webar-event-poster",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5173",
    "compile-targets": "node tools/compile-targets.mjs"
  },
  "dependencies": {
    "three": "^0.160.0",
    "vite": "^5.1.0"
  }
}
